# Enable Rewrite Engine
RewriteEngine On

# Redirect all requests to HTTPS and non-www
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^www\.
RewriteRule ^(.*)$ https://tmatic.org/$1 [L,R=301]
